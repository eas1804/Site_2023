apt-get install hdparm


hdparm -t /dev/sda

Команда hdparm -t выполняет тестирование скорости считывания 
последовательных блоков данных с диска и выводит результаты
в виде скорости передачи данных в МБ/с.


hdparm -Tt /dev/sda